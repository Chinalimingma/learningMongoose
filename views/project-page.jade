extends layout

block content
    h1 Mongoose Project Management
    h2= projectName
    p Created by: #{createdBy}
    if tasks.length > 0
        ul 
            for task in tasks
                li
                    a(href="/project/#{projectID}/task/edit/#{task._id}", title="#{task.taskDesc}") #{task.taskName}
    